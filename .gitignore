# The editor bundle in assets/vendor/ IS committed on purpose: the site must work
# with no build step. Only the build's own dependencies are ignored.
tools/node_modules/
tools/package-lock.json

# java/src IS committed (it is the browsable Java tree). Build output is not.
java/out/
java/sources.txt
*.class

# scratch
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
tmp/
